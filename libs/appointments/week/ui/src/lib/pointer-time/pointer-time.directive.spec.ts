import { PointerTimeDirective } from './pointer-time.directive';

describe('PointerTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new PointerTimeDirective();
    expect(directive).toBeTruthy();
  });
});
